Прочитай файл `/Users/alexei.kalinin/Documents/VibeCoding/What2Eat/.claude/changelog.md` (если он не существует — создай его с заголовком `# What2Eat — Changelog`).

Затем запроси у пользователя (или возьми из аргументов $ARGUMENTS):
- Тип записи: `fix` (исправление бага), `feature` (новая функциональность), `issue` (открытый вопрос), `decision` (архитектурное решение), `status` (текущий статус работ)
- Описание изменения/проблемы
- (Опционально) Решение или как воспроизвести

После сбора данных добавь запись в changelog.md в следующем формате:

```markdown
## [YYYY-MM-DD] <тип>: <краткое описание>

**Проблема:** ...
**Решение:** ...
**Файлы:** ...
**Статус:** открыт / закрыт
```

Если аргументы переданы через $ARGUMENTS, разбери их как: `<тип> <описание>`.

В конце выведи последние 5 записей из changelog.md чтобы пользователь видел текущий статус.
